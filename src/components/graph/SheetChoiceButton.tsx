import React, { FC } from 'react';

type SheetChoiceButtonProps = {};

const SheetChoiceButton: FC<SheetChoiceButtonProps> = ({}) => {
	return <div>SheetChoiceButton</div>;
};

export default SheetChoiceButton;
