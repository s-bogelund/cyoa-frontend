import React, { FC } from 'react';
import FilterWrapper from './FilterWrapper';

type AgeFilterProps = {};

const AgeFilter: FC<AgeFilterProps> = ({}) => {
	return <FilterWrapper>AgeFilter</FilterWrapper>;
};

export default AgeFilter;
